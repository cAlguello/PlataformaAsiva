import { BuscadorFilterPipe } from './buscador-filter.pipe';

describe('BuscadorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
